# Subtitle Viewer — Claude Code Guidelines

## 실행 원칙

- 계획·분석 후 임의로 실행하지 않는다. 반드시 사용자의 명시적 실행 지시를 받은 후 작업을 시작한다.
- 파괴적이거나 되돌리기 어려운 작업(파일 삭제, 서비스 재시작, 배포, git push 등)은 실행 전 반드시 사전 확인을 받는다.
- 제안이나 계획을 제시한 뒤 사용자의 승인 없이 다음 단계로 넘어가지 않는다.

## 테스트 서버 실행 안내
- 테스트 서버를 띄운 뒤 URL을 출력할 때 `localhost` 대신 **호스트의 실제 IP**를 사용할 것
- 헤드리스 환경에서 다른 기기로 접근하므로 `localhost`는 의미가 없음
- IP 확인 방법: `hostname -I | awk '{print $1}'`
- 예시 출력: `http://192.168.12.46:8091/`
